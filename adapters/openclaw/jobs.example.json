{
  "version": 2,
  "runtime": "openclaw",
  "template": true,
  "status": "adapter-contract-only",
  "note": "glance_brief v0.3.0 ships a runtime-independent core and a verified Hermes model adapter. A complete OpenClaw model adapter has not yet been implemented, so this file intentionally advertises no runnable jobs. An OpenClaw adapter must execute one model call, return raw JSON to glance_brief.run_pipeline through the model_runner interface, preserve fail-closed artifacts, and deliver only report.md.",
  "jobs": []
}
