{
  "version": 2,
  "runtime": "hermes",
  "template": true,
  "note": "Run install/install.py first. Each script is relative to $HERMES_HOME/scripts/. no_agent disables only the outer scheduler agent; the installed Hermes runtime adapter performs one model call, then the shared core validates and renders the report. Configure model/provider through GLANCE_BRIEF_MODEL and GLANCE_BRIEF_PROVIDER if runtime defaults are not suitable. Do not commit real delivery identifiers.",
  "jobs": [
    {
      "name": "agents-report daily",
      "schedule_utc": "0 2 * * *",
      "script": "glance-brief/agents-report.py",
      "no_agent": true,
      "deliver": "<platform>:<chat_id>"
    },
    {
      "name": "noon-news",
      "schedule_utc": "30 4 * * *",
      "script": "glance-brief/noon-news.py",
      "no_agent": true,
      "deliver": "<platform>:<chat_id>"
    }
  ]
}
